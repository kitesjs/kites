language: node_js

git:
  depth: 5

node_js:
  - '8'
  - '10'
  - '12'

before_install:
# show product version
  - npm i -g npm@latest
  - nvm --version
  - node --version
  - npm --version

install:
  - npm ci
  - npm run build:dev

script:
  - npm run lint
  - npm test
